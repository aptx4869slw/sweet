<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.song.sweetgirl.dao.UserDAO">
    <resultMap id="userMap" type="com.song.sweetgirl.model.User">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="findUser" resultMap="userMap" parameterType="com.song.sweetgirl.model.User">
        SELECT * FROM User
        WHERE username = #{username} AND password = #{password}
    </select>

    <select id="findByUserName" resultMap="userMap" parameterType="com.song.sweetgirl.model.User">
        SELECT * FROM User
        WHERE username = #{username}
    </select>

    <insert id="saveUser" parameterType="com.song.sweetgirl.model.User">
        INSERT INTO User (username, password)
        VALUES(#{username}, #{password})
    </insert>

</mapper>
